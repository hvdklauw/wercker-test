box: python:3.4
build:
    steps:
        - script:
            name: do something
            code: |
                python --version > version.txt
                cp version.txt $WERCKER_OUTPUT_DIR

deploy:
    steps:
        - script:
            name: list
            code: cat version.txt
