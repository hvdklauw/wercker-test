box: python:3.4
build:
    steps:
        - script:
            name: do something
            code: |
                python --version > version.txt
    after-steps:
        - script:
            name: bla
            code: |
              ls -al $WERCKER_REPORT_ARTIFACTS_DIR

deploy:
    steps:
        - script:
            name: show
            code: ls -al
