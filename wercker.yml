box: python:3.5
build:
    steps:
        - script:
            name: do something
            code: |
                python --version > $WERCKER_REPORT_ARTIFACTS_DIR/env.txt
    after-steps:
        - script:
            name: bla
            code: |
              env >> $WERCKER_REPORT_ARTIFACTS_DIR/env.txt
