box: python3.4
build:
    steps:
        - script:
            name: do something
            code: |
                python --version > version.txt
deploy:
    steps:
        - script:
            name: list
            code: ls -al
