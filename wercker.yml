box: python:3.5
build:
    steps:
        - script:
            name: step
            code: env
    after-steps:
        - script:
            name: after
            code: env


deploy:
    steps:
        - script:
            name: step
            code: env
    after-steps:
        - script:
            name: after
            code: env
